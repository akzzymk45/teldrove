name: Ultimate Movie Uploader

on:
  workflow_dispatch:
    inputs:
      movie_url:
        description: 'Direct Download URL of the Movie'
        required: true
        type: string
      folder_name:
        description: 'Target Folder in TelDrive'
        required: false
        default: 'github'
        type: string

jobs:
  upload:
    runs-on: ubuntu-latest
    steps:
      - name: 🚀 Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.22'

      - name: 📦 Clone Core TelDrive Backend (Latest Code)
        uses: actions/checkout@v4
        with:
          repository: tgdrive/teldrive
          path: teldrive-backend

      - name: ⚙️ Install Tools (Aria2 & Custom Rclone)
        run: |
          sudo apt-get update && sudo apt-get install -y aria2
          wget -q https://github.com/tgdrive/rclone/releases/download/v1.73.1/rclone-v1.73.1-linux-amd64.zip
          unzip -q rclone-v1.73.1-linux-amd64.zip
          sudo mv rclone-v1.73.1-linux-amd64/rclone /usr/bin/rclone
          sudo chmod +x /usr/bin/rclone
          rm -rf rclone-v1.73.1-linux-amd64*

      - name: 🌐 Start TelDrive Server in Background
        run: |
          cd teldrive-backend
          # Running from latest source code fixes the Supabase PgBouncer crash!
          go run main.go run \
            --db-data-source="${{ secrets.TELDRIVE_SUPABASE_DSN }}" \
            --jwt-secret="${{ secrets.TELDRIVE_JWT_SECRET }}" \
            --tg-app-id="${{ secrets.TELDRIVE_TG_APP_ID }}" \
            --tg-app-hash="${{ secrets.TELDRIVE_TG_APP_HASH }}" \
            --tg-uploads-encryption-key="${{ secrets.TELDRIVE_TG_UPLOADS_ENCRYPTION_KEY }}" &
          
          sleep 15
          
      - name: 🔑 Configure Rclone Authentication
        run: |
          mkdir -p ~/.config/rclone
          cat <<EOF > ~/.config/rclone/rclone.conf
          [teldrive]
          type = teldrive
          api_host = http://127.0.0.1:8080/
          access_token = ${{ secrets.TELDRIVE_TOKEN }}
          chunk_size = 100M
          upload_concurrency = 8
          encrypt_files = false
          random_chunk_name = true
          EOF

      - name: 📥 Download Movie to Azure Runner (Auto-Naming)
        run: |
          mkdir movie_download
          cd movie_download
          aria2c -x 16 -s 16 --content-disposition --summary-interval=10 --console-log-level=warn "${{ github.event.inputs.movie_url }}"

      - name: ☁️ Upload Movie to Telegram via TelDrive
        run: |
          rclone copy ./movie_download/ teldrive:${{ github.event.inputs.folder_name }}/ \
            --multi-thread-streams=8 \
            --multi-thread-chunk-size=100M \
            --use-mmap \
            --stats 10s --stats-one-line -v
